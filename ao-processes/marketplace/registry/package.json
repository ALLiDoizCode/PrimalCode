{
  "name": "provider-registry-process",
  "version": "1.0.0",
  "description": "AI inference marketplace provider registry AO process",
  "main": "build/main.lua",
  "scripts": {
    "build": "node scripts/build.js",
    "deploy": "node scripts/deploy-local.js",
    "test": "busted tests/",
    "clean": "rm -rf build/",
    "rebuild": "npm run clean && npm run build"
  },
  "keywords": ["ao", "marketplace", "registry", "inference", "ai"],
  "author": "PrimalCode",
  "license": "MIT",
  "devDependencies": {
    "busted": "^2.0.0"
  }
}